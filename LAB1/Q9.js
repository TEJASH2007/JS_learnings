// Que : Write a JavaScript program to convert Celsius to Fahrenheit, ask the 
//       Celsius value to the user.

let celcius = prompt("Celcius");  // string
celcius = Number(celcius);

let ferenhit = (1.8)*(celcius) + 32;
console.log(ferenhit)